# JavaScript Advanced Practice Questions (101–200)

This document contains **100 intermediate-to-advanced JavaScript questions** focused on **problem‑solving, modern JS features, DOM manipulation, algorithms, and real‑world use cases**.

These questions are ideal for:
- 🚀 Leveling up JavaScript skills
- 🧠 Interview preparation
- 💻 Building real-world confidence

---

## 1. Intermediate Console and Variables (10 Questions)
1. Create a program that calculates and logs the result of `(5 + 3) * 2 - 8 / 4`.
2. Log the value of a variable before and after incrementing it using `++`.
3. Write a program that swaps the values of two variables without using a third variable.
4. Create a variable with a template literal that includes your name and age.
5. Use `console.table()` to display the object `{ name: "John", age: 30, city: "New York" }`.
6. Create a program that uses `console.group()` to group related logs.
7. Use `console.time()` and `console.timeEnd()` to measure the execution time of a simple loop.
8. Log a variable’s value dynamically inside a string.
9. Use `console.trace()` to debug a function call stack.
10. Create and log an object dynamically using computed property names.

---

## 2. Advanced Loops (10 Questions)
11. Write a nested loop to generate a multiplication table (1–10).
12. Use a `for...in` loop to iterate through object properties.
13. Calculate the sum of all even numbers between `1` and `100`.
14. Use a `for...of` loop to count vowels in a string.
15. Create a loop that breaks when a number divisible by `7` is found.
16. Write a loop that skips numbers divisible by `3` but logs all others up to `20`.
17. Use a `do...while` loop to prompt until a positive number is entered.
18. Generate the following pyramid pattern using nested loops:

```
   *
  ***
 *****
*******
```

19. Reverse the string `"JavaScript"` using a loop.
20. Calculate the product of all numbers in an array using a loop.

---

## 3. Arrays and Objects (10 Questions)
21. Merge two arrays and remove duplicate values.
22. Find the second largest number in an array.
23. Remove all falsy values from an array.
24. Use `.reduce()` to find the sum of array elements.
25. Rotate an array to the left by `n` positions.
26. Use `.find()` to locate the first even number in an array.
27. Flatten a nested array recursively.
28. Count the frequency of each element in an array.
29. Use `.some()` and `.every()` to validate array conditions.
30. Sort an array of objects by a specific property.

---

## 4. Functions and Closures (10 Questions)
31. Write a function that returns another function to calculate power.
32. Create a recursive Fibonacci function.
33. Use closures to create a counter.
34. Implement memoization in a function.
35. Create a debounce function.
36. Implement a throttle function.
37. Write a higher‑order function that applies a callback to each array element.
38. Calculate the GCD of two numbers.
39. Execute an array of functions sequentially.
40. Implement currying for a mathematical operation.

---

## 5. Strings and Regular Expressions (10 Questions)
41. Reverse words in a sentence without reversing the sentence order.
42. Validate an email address using regex.
43. Check if a string contains only digits.
44. Extract all numbers from a string.
45. Find all occurrences of a word using `.match()`.
46. Capitalize the first letter of each word.
47. Remove duplicate characters from a string.
48. Replace multiple spaces with a single space using regex.
49. Check if two strings are anagrams.
50. Count word frequency in a string.

---

## 6. Intermediate DOM Manipulation (10 Questions)
51. Dynamically create `10` buttons on a webpage.
52. Log a button’s `id` when clicked.
53. Change a `<div>` background color on hover.
54. Add a CSS class dynamically on button click.
55. Toggle element visibility.
56. Count the number of `<p>` tags on a page.
57. Disable all form input fields.
58. Clone an element and append it to the DOM.
59. Display clicked list item text.
60. Validate a form using JavaScript.

---

## 7. Intermediate Algorithms (10 Questions)
61. Merge two sorted arrays.
62. Implement binary search.
63. Check if two strings are one edit away.
64. Find the longest common prefix.
65. Check if a string can form a palindrome.
66. Find the intersection of two arrays.
67. Implement the Sieve of Eratosthenes.
68. Generate all subsets of an array.
69. Calculate power iteratively.
70. Implement quicksort.

---

## 8. Advanced Error Handling (10 Questions)
71. Handle division by zero.
72. Catch invalid JSON parsing errors.
73. Throw a custom error for invalid input.
74. Handle rejected promises.
75. Use `finally` for cleanup.
76. Retry a failed API call up to `3` times.
77. Use `try...catch` inside an async function.
78. Handle syntax errors dynamically.
79. Ensure a variable is defined before usage.
80. Check file existence before reading.

---

## 9. Advanced Array and Object Manipulation (10 Questions)
81. Group objects by a property.
82. Transform objects using `.map()`.
83. Deep clone an object.
84. Merge multiple arrays into a unique array.
85. Count property occurrences in objects.
86. Sort objects by multiple properties.
87. Create a nested object from flat data.
88. Use `.reduce()` to group values.
89. Find differences between object arrays.
90. Filter and transform objects in one step.

---

## 10. Real‑World Use Cases (10 Questions)
91. Fetch and display data from a mock API.
92. Build a shopping cart system.
93. Implement pagination.
94. Dynamically update a chart.
95. Search and highlight text on a webpage.
96. Create a stopwatch.
97. Validate password strength.
98. Build an image slider.
99. Sort and display API data.
100. Auto‑save form data using `localStorage`.

---

### 📌 How to Practice
- Solve **5–10 questions daily**
- Write clean, commented code
- Re‑implement using ES6+
- Convert selected problems into mini projects

🔥 Want next?
- ✅ Full **solutions + explanations**
- 🧠 **Hints only** version
- 🗓️ **Advanced 45‑day JS roadmap**
- 📂 GitHub‑ready **folder structure**

